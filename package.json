{
  "name": "database-service",
  "version": "1.0.0",
  "description": "A database service provided for bot commander.",
  "main": "./dist/database.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Wayv, Doggo",
  "license": "Apache-2.0",
  "devDependencies": {
    "@types/node": "^16.0.0",
    "typescript": "^4.3.5"
  }
}
